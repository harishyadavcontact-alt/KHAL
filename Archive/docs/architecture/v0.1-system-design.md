# Khal v0.1 Architecture

Historical snapshot (archived):

- v0.1 treated `Genesis.xlsx` as canonical source.
- Companion normalization sheets (`_khal_*`) and `_khal_meta` existed in that phase.
- Sync engine enforced stale/conflict refresh gate.
- UI was narrative-first with mission/vision framing.

Current authority is documented in `README.md` and `docs/architecture/sqlite-architecture.md` (SQLite-first v0.3 runtime).
