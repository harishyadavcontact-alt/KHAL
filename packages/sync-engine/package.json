{
  "name": "@khal/sync-engine",
  "version": "0.1.0",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "tsc --noEmit -p tsconfig.json",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run"
  },
  "dependencies": {
    "@khal/domain": "file:../domain",
    "@khal/sqlite-core": "file:../sqlite-core",
    "better-sqlite3": "^11.8.1"
  },
  "devDependencies": {
    "vitest": "^3.0.9"
  }
}
